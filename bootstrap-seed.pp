package {
    'vim':
        ensure => installed;
    'vim-puppet':
        ensure => installed;
    'puppetmaster-passenger':
        ensure => installed;
    'syslog-ng':
        ensure => installed;
    'bind9':
        ensure => installed;
    'git':
        ensure => installed;   
}
